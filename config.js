// CST Report config for production on Hostinger

// Frontend will call your secure PHP proxy instead of Google Apps Script directly.
const API_URL = "https://csp-nobel.com/api/cst.php";

// Optional: client-side API key (for basic abuse control). You can change this if you want.
const CLIENT_API_KEY = "cst_front_001";
